import styled from "styled-components";

export const StyledLink = styled.a`
    color: white;
    font-size: 2rem;
    line-height: 50%;
    line-height: normal;
    
    &:hover {
        text-decoration: underline;
        cursor: pointer;
    }
`;